# Feature: vault-search

- ✅ Audit browser FTS via vault `search` port (rebuildable index)
- ✅ Filter by query string; empty query shows recent audit list
- ✅ Results open as path + type + title (PD-13)

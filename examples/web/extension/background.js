/** MV3 stub — user clicks action; host page handles staged Evidence (no remote upload). */
chrome.action.onClicked.addListener(() => {
  // Selection capture is performed in the PWA / share-target path, not via silent scrape.
});

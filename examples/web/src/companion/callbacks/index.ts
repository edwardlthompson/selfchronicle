export function callbackCard(sourceId: string): string {
  return `Remembering from ${sourceId} — with source.`;
}
export function repairRitualCopy(): string {
  return "Thanks for the correction. Your edit wins.";
}

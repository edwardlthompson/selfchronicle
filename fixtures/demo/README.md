# Demo fixtures

Synthetic seed data for the guest/demo vault (`examples/web/src/demo-vault/`).

- No real PII — names and events are fictional
- Safe to commit; used by unit tests and local demos
- Prefer regenerating via `seedDemoVault()` rather than hand-editing binaries
